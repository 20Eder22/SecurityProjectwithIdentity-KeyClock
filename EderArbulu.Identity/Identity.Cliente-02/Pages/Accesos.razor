
@page "/accesos"
@using Services
@inject ConfigService ConfigService

<h3>Accesos</h3>

@if (_error)
{
    <RedirectToLoginOrUnauthorized />
}
else if (_resultado == null)
{
    <p><em>Cargando...</em></p>
}
else
{
    <pre>@_resultado</pre>
}

@code {
    private string? _resultado;
    private bool _error;

    protected override async Task OnInitializedAsync()
    {
        try
        {
            _resultado = await ConfigService.ObtenerAccesosAsync();
        }
        catch (UnauthorizedAccessException)
        {
            _error = true;
        }
    }
}